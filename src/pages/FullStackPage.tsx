import { Container, Heading } from "@chakra-ui/react";
import FullStackSection from "../components/Fullstack Section/FullStackSection";

export const FullStackPage = () => {
  return (
    <Container maxW={"6xl"} py={12}>
      <FullStackSection />
    </Container>
  );
};
